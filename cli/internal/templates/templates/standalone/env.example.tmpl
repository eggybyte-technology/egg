# ==============================================================================
# EggyByte Standalone Service Environment Configuration
# ==============================================================================
#
# This file provides example environment variables for the standalone service.
# Copy this file to .env and customize the values for your environment.
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your configuration
#   egg standalone run
#
# ==============================================================================

# ------------------------------------------------------------------------------
# Service Configuration
# ------------------------------------------------------------------------------

# Service name and version
SERVICE_NAME={{.ServiceName}}
SERVICE_VERSION=0.1.0
ENV=development

# Log level: debug, info, warn, error
LOG_LEVEL=info

# ------------------------------------------------------------------------------
# Server Ports
# ------------------------------------------------------------------------------

# HTTP/Connect server port
HTTP_PORT=8080

# Health check endpoint port
HEALTH_PORT=8081

# Prometheus metrics endpoint port
METRICS_PORT=9091

# ------------------------------------------------------------------------------
# Database Configuration (if using database)
# ------------------------------------------------------------------------------

# Database driver: mysql, postgres, sqlite
DB_DRIVER=mysql

# Database connection string (DSN)
# MySQL: user:password@tcp(host:port)/dbname?charset=utf8mb4&parseTime=True&loc=Local
# PostgreSQL: host=localhost port=5432 user=postgres password=postgres dbname=mydb sslmode=disable
# SQLite: file:/path/to/database.db
DB_DSN=user:password@tcp(localhost:3306)/{{.ServiceName}}?charset=utf8mb4&parseTime=True&loc=Local

# Database connection pool settings
DB_MAX_IDLE=10
DB_MAX_OPEN=100
DB_MAX_LIFETIME=1h

# ------------------------------------------------------------------------------
# Observability (Optional)
# ------------------------------------------------------------------------------

# OpenTelemetry endpoint for traces and metrics
# Leave empty to disable OTLP export
OTEL_EXPORTER_OTLP_ENDPOINT=

# ------------------------------------------------------------------------------
# Application-Specific Configuration
# ------------------------------------------------------------------------------

# Add your application-specific environment variables here
# Example:
# API_KEY=your-api-key-here
# FEATURE_FLAG_X=true
# MAX_UPLOAD_SIZE=10485760

